# --- OS / editor ---
*~
.DS_Store
.DS_STORE

# JetBrains / WebStorm
.idea/
*.iml
*.ipr
*.iws

# --- Python ---
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# --- Node (if ever added) ---
node_modules/

# ======================================================================
# DATA & GENERATED ARTIFACTS  (code-only repo — regenerate via model/start.sh)
# ======================================================================

# Raw AudioMNIST dataset (~2.7 GB) and intermediate training artifacts
model/data/
model/artifacts/

# Web model export generated by model/export_web.py
# (TF.js model, 3D layout, and bundled sample clips are derived data, not code).
# Comment these out if you want a clone to run the demo without retraining.
js/nn/tfjs_model/
js/nn/layout.json
js/nn/samples.json
